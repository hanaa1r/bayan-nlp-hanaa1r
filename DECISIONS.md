## Day 1 — Tokenizer decision

Checkpoint/tokenizer: google-bert/bert-base-multilingual-cased (مرمّز وأوزان من نفس الـ checkpoint). المرمّز المحلي WordPiece تعليمي فقط.

Corpus slice: 5 جمل اصطناعية ثنائية اللغة من الدفتر (SEED=42)، بعد profile محافظ وإخفاء PII.

Arabic fertility [MEASURED]: 1.75 على الجملة المختلطة (7 رموز / 4 كلمات) بـ mBERT. المرمّز المحلي: 1.36 متوسط على 5 عينات.

English fertility [MEASURED]: 1.00 (5 رموز / 5 كلمات) بـ mBERT.

Truncation rate at max_length=10 [MEASURED]: 0% على 5 جمل قصيرة بالمرمّز المحلي.

Known limitation:
- العينة 5 جمل فقط، توضيحية لا إحصائية.
- fertility المرمّز المحلي غير صالح للمقارنة بين اللغتين لأن قاموسه 27 رمزًا ويعطي [UNK] كثيرة.
- sentencizer يقطع بعد "د." — "راجع د. أحمد." انقسمت لجملتين، وتحتاج استثناء قبل الإنتاج.
- قياس truncation يجب أن يتم قبل enable_truncation، وإلا كل تسلسل يصير بطول max_length والنتيجة 0% حتمًا.

Decision and reason: mBERT مع MAX_LENGTH=12 في هذا الدفتر، وprofile محافظ بلا حذف تشكيل ولا توحيد ألف لأن أيًا منهما لم تثبت فائدته لمهمة محددة بعد. إعادة القياس مطلوبة على corpus حقيقي قبل تثبيت max_length.


## Day 2 — Classification, NER & QA decisions

**Checkpoint والسبب:** distilbert/distilbert-base-multilingual-cased — مشفّر متعدد اللغات يغطي العربية والإنجليزية معًا، وأخف من mBERT الكامل فيناسب CPU. المرمّز والأوزان من نفس الـ checkpoint في المهام الثلاث.

**نوع التنفيذ:** partial_finetune_cpu — جمّدت معظم المشفّر وحدّثت آخر Transformer block مع رأس المهمة، لأن الجهاز CPU ولا GPU متاح. في التصنيف: 7,681,540 معامل قابل للتدريب من 135,327,748.

**Split strategy ودليل عدم التسرب:** split مجمّد في ملف البيانات (train 24 / validation 8 / test 8)، مع group_id يمنع تسرب الحالات المتشابهة. الدالة validate_splits تتحقق أن كل مجموعة تنتمي إلى split واحد فقط وأن الفئات الأربع موجودة في كل split. النتيجة: **group_overlap = 0** على 20 مجموعة.

**Baseline وTransformer metric [MEASURED_SMOKE]:**
- TF-IDF + LinearSVC — validation macro-F1 = 0.6667، test macro-F1 = 0.7333
- Transformer (الحقبة المختارة 9 من 12) — validation macro-F1 = 1.0000، test macro-F1 = 0.8667، accuracy = 0.875
- الفرق على validation: **+0.3333**
- خطأ واحد على test: "الخدمة الإلكترونية واضحة وسريعة" صُنّفت health بدل digital_service. كلمة "الخدمة" مشتركة بين الفئتين في التدريب، والنظير الإنجليزي صُنّف صحيحًا.

**NER alignment policy:** أول subword من كل كلمة يحمل الـ label، وكل ما عداه يأخذ **-100** — أي special tokens ([CLS]/[SEP]/padding) والأجزاء الفرعية التابعة. القياس على **مستوى الكيان** لا على مستوى الرمز: حدود الكيان يجب أن تطابق تمامًا وإلا احتُسب خطأ كاملًا. النتيجة [MEASURED_SMOKE]: entity F1 = 0.5714 (precision 0.667، recall 0.500) على 4 كيانات حقيقية و3 متنبأ بها، بعد 12 حقبة و48 خطوة (mean loss 0.862).

**QA null policy:** أمثلة بلا إجابة تُعيَّن إلى موضع [CLS] في start و end. في post-processing يُحسب null_score من logits الموضع صفر، ويُقارن بأفضل span صالح؛ إذا كان الفارق أكبر من العتبة تُعاد `None` مع سبب صريح. الاختبارات الحتمية: span صالح أعاد "الرياض"، وحالة no-answer أعادت None بالسبب no_answer_in_context وmargin = 6.0. QA loss = 3.645 بعد 3 خطوات فقط.

**ما لا تستطيع العينة الصغيرة إثباته:**
- 40 صف تصنيف و12 جملة NER و10 أمثلة QA لا تكفي لتقدير جودة إنتاجية.
- validation من 8 أمثلة استُخدم لاختيار الحقبة، فرقم test متفائل بطبيعته.
- خطأ واحد على test يساوي 12.5%، فالفروق الصغيرة غير معنوية.
- QA بعد 3 خطوات لم يتعلم شيئًا ذا دلالة؛ الاختبارات الناجحة تخص صحة post-processing لا جودة النموذج.
- لتقدير موثوق نحتاج: بيانات أكبر، عدة بذور مع mean ± range، شرائح لغوية منفصلة (ar مقابل en)، وtest مجمّد لا يُقرأ أثناء الضبط.


## Day 3 — Arabic profile decision

**Profile + version + backend:** profile اسمها `search`، الإصدار 1.0.0، backend هو camel-tools==1.6.0. القواعد: حفظ نسخة العرض، إخفاء PII، تطبيع Unicode بلا compatibility، حذف التطويل والتشكيل، توحيد الألف والألف المقصورة، **مع الإبقاء على التاء المربوطة**.

**ما تغيّر وما حُفظ:** `display_text` تبقى كما وردت ولا يُكتب فوقها؛ `model_text` مشتقة وقابلة لإعادة البناء. مثال: "إِدَارَةُ الحِساب" تبقى كما هي في نسخة العرض وتصير "ادارة الحساب" في نسخة البحث. الأربع golden cases نجحت، و20 صفًا عولجت (Gulf 9، MSA 9، Arabizi 2).

**لماذا لا profile واحدة لكل الـcheckpoints:** كل checkpoint دُرِّب على تمثيل نصي معيّن؛ حذف التشكيل يفيد البحث لكنه قد يمحو تمييزًا تحتاجه مهمة أخرى. والأهم: تغيير الـprofile بعد بناء الفهرس يعني أن corpus وquery لم يعودا يتبعان العقد نفسه، فتنهار المطابقة صامتة.

**Arabizi:** يُمرَّر كما هو في مسار مستقل (`arabizi_passthrough`) ولا يُحوَّل بقواعد تخمينية. الكشف heuristic شفاف (حروف لاتينية + أرقام 2/3/5/6/7/8/9 + صفر حروف عربية)، **وليس مصنّف لهجة مدرَّبًا**. وسم `variant` في البيانات تعليمي أُنشئ مع العينة، لا تنبؤ.

**مقارنة النموذجين [MEASURED_SMOKE]:** على نفس الـsplit والبذرة (42)، بتدريب آخر encoder block + رأس التصنيف، 40 خطوة لكل نموذج:
- distilbert-base-multilingual-cased → Gulf test macro-F1 = 0.0000
- CAMeL-Lab/bert-base-arabic-camelbert-da → Gulf test macro-F1 = 0.6667

**الحدود:** validation من 4 أمثلة وGulf test من 4 أمثلة، بذرة واحدة. كل مثال يساوي 25%، فالفرق وصفي لا إحصائي. النتيجة **متسقة مع التوقع** — نموذج مدرَّب على العربية واللهجات يقرأ نصًا خليجيًا أفضل — لكنها **لا تثبت تفوقًا عامًا**. لتأكيده نحتاج عينة أكبر، عدة بذور، وشرائح مجمدة أوسع


## Day 3 — Semantic search decision

**Embedding model + license:** `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`، بُعد 384، Apache-2.0 حسب model card على Hugging Face. Re-ranker: `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`.

**Index type ولماذا Flat:** `IndexFlatIP` — بحث دقيق على 24 متجهًا. بعد تطبيع L2، الضرب النقطي يرتّب بنفس ترتيب cosine similarity. ANN مثل HNSW/IVF قرار له معنى عند أحجام أكبر وبعد مقارنة جودة/سرعة موثقة؛ على 24 سجلًا لا يقدّم شيئًا ويضيف تعقيدًا.

**k وthreshold ومكان الضبط:** k=3 للاسترجاع، و6 مرشحين للـre-ranking. عتبة no-answer ضُبطت على **validation فقط** (threshold = 0.4592، دقة validation = 1.000 على 10 استعلامات)، ثم جُمّدت قبل قراءة test (دقة test = 1.000 على 8 استعلامات). لم تُعدَّل بعد رؤية test.

**Preprocessing symmetry:** corpus وquery يمران بنفس الدالة — profile عربية `search/1.0.0` من دفتر 05 للنصوص العربية، وNFC وتوحيد مسافات للإنجليزية. أي اختلاف بين الجهتين يعني مقارنة فضاءين مختلفين. الـmanifest يوثّق النموذج والبُعد والتطبيع وsha256 للبيانات، وأي تغيير في هذه الحقول يستلزم إعادة بناء الفهرس.

**Recall/MRR والشرائح [MEASURED_SMOKE]:**

| الشريحة | n | Recall@3 | MRR@3 |
|---|---|---|---|
| الإجمالي | 6 | 1.000 | 0.667 |
| language=ar | 3 | 1.000 | 0.500 |
| language=en | 3 | 1.000 | 0.833 |
| retrieval_mode=monolingual | 4 | 1.000 | 0.750 |
| retrieval_mode=cross_lingual | 2 | 1.000 | 0.500 |

كل الشرائح موسومة SMALL_SLICE.

**قراءة النتيجة:** Recall@3 كامل في كل الشرائح — الإجابة الصحيحة تظهر دائمًا ضمن الثلاثة. لكن MRR يكشف أن الترتيب أضعف للعربية (0.500) منه للإنجليزية (0.833)، وأضعف للاستعلامات عابرة اللغة (0.500). هذا متسق مع ضعف تمثيل العربية في النماذج متعددة اللغات، لكن كل شريحة من ٢-٤ أمثلة، فالفرق وصفي لا إحصائي.

**Re-ranking [MEASURED_SMOKE]:** MRR@3 ارتفع من 0.667 إلى 0.722 (+0.056) على 6 استعلامات قابلة للإجابة، مع استبعاد warm-up من القياس. القرار: `ADOPT_FOR_EXPERIMENT` — مكسب مقيس لكن على عينة صغيرة جدًا، فهو مرشح للتجربة لا قرار إنتاج. الـcross-encoder يعيد ترتيب top-6 فقط لا الفهرس كاملًا، لأن تكلفته تنمو خطيًا مع عدد المرشحين.


## Day 3 — Evaluation decision

**Headline metric:** Macro-F1 على 36 مثال validation من `bayan_day3_predictions.csv`، موسومة **COURSE_FIXTURE** — تنبؤات تعليمية من الدورة، ليست مخرجات نموذجي ولا benchmark. لم يُستخدم أي frozen test في بناء التصنيف أو اختيار الإصلاحات.

| النظام | Macro-F1 | 95% CI |
|---|---|---|
| A | 0.781 | [0.621, 0.898] |
| B | 0.782 | [0.617, 0.904] |

**CI verdict:** الفرق المزدوج B − A = +0.0012، بمجال ثقة **[−0.105, +0.100]**. المجال يشمل الصفر، فهذه العينة **لا تدعم ادعاء أفضلية اتجاهية** لأي من النظامين. الفارق المرصود (أقل من نقطة مئوية واحدة) أصغر بكثير من عدم اليقين في القياس. زيادة `n_boot` لن تضيّق المجال — العلاج الوحيد بيانات أكثر.

**الشرائح المختارة والسبب:** اخترت `language` و`variant` و`length_bucket` لأنها تفصل محاور فشل متوقعة: العربية مقابل الإنجليزية، اللهجة مقابل الفصحى، والنصوص القصيرة مقابل الطويلة. الحد التعليمي للمراجعة 15 مثالًا؛ ما دونه يُوسم SMALL_SLICE.

| الشريحة | n | Macro-F1 | 95% CI | الوسم |
|---|---|---|---|---|
| ALL | 36 | 0.782 | [0.611, 0.896] | — |
| language=ar | 24 | 0.758 | [0.561, 0.927] | — |
| language=en | 12 | 0.829 | [0.500, 1.000] | SMALL_SLICE |
| variant=MSA | 12 | 0.837 | [0.530, 1.000] | SMALL_SLICE |
| variant=English | 12 | 0.829 | [0.497, 1.000] | SMALL_SLICE |
| **variant=Gulf** | 12 | **0.658** | [0.294, 0.895] | SMALL_SLICE |
| length=short | 18 | 0.829 | [0.625, 1.000] | — |
| **length=long** | 18 | **0.526** | [0.392, 0.831] | — |

**ما يخفيه المتوسط:** المتوسط 0.782 يبدو مقبولًا، لكن شريحتين تنزلان تحته بوضوح — النصوص الطويلة (0.526) واللهجة الخليجية (0.658). شريحة الطول ليست SMALL_SLICE (n=18) ومجالها [0.392, 0.831] لا يلامس المتوسط، فهي أقوى إشارة عندي. مجالات الشرائح الصغيرة واسعة جدًا (بعضها يمتد نصف المدى الممكن) وتُقرأ كأسئلة لا كأحكام.

**الاختبارات السلوكية:** 3 من 6 نجحت (pass rate = 0.50). الفشل الثلاثة كلها من نوع واحد: نصوص عن نتيجة/موعد صحي أو مسار حافلة صُنّفت في فئة خدمة أخرى — أي أن الإشارة السياقية (تطبيق، خريطة) غلبت نية المجال. المقياس على مجموعة البيانات يقول «كم»؛ الاختبار السلوكي يقول «هل يحترم العقد في حالة محددة» — و0.50 على ستة عقود واضحة أخطر من 0.782 متوسطًا.

**Taxonomy (يدوية على validation):** 8 أخطاء مصنّفة — `dialect_gap` ×3، `hard_or_ambiguous` ×3، `class_confusion` ×2.

**Top-3 fixes مرتبة بدليل:**

1. **تغطية اللهجة الخليجية في الصحة والنقل** — الدليل: `dialect_gap` أكثر وسم متكرر (3)، وشريحة Gulf أدنى شريحة variant (0.658). التغيير: جمع ومراجعة أمثلة خليجية موجّهة لهاتين الفئتين. اختبار القبول: حالات سلوكية جديدة تنجح + ارتفاع الحد الأدنى لمجال ثقة شريحة Gulf.

2. **الخلط بين ألفاظ التطبيق/الحالة وفئات المجال** — الدليل: EV-019 (حالة تصريح → digital_service) وEV-020 (مسار حافلة → digital_service). التغيير: إضافة أمثلة تقابلية تفصل «تطبيق/حالة» عن «تصريح/مسار»، ومراجعة دليل الوسم. اختبار القبول: مقارنة مزدوجة تُظهر تحسنًا بلا تراجع في الشرائح الأخرى.

3. **الطلبات القصيرة الناقصة السياق** — الدليل: `hard_or_ambiguous` ×3، معظمها نصوص قصيرة بلا اسم الفئة (مثل «المرفق انرفض»، «The request is stuck»). التغيير: طلب سياق إضافي أو الامتناع عن التصنيف عند ثقة منخفضة. اختبار القبول: مجموعة سلوكية للغموض.

**ما لا تثبته هذه القياسات:** 36 مثالًا وتنبؤات fixture تعليمية لا تقدّر جودة إنتاجية. مجالات الثقة واسعة، وكل شريحة variant من 12 مثالًا. النتائج تصف هذه العينة فقط، ولا تُنسب إلى نموذجي ولا إلى مجتمع مستخدمين.

**ما لا تثبته هذه القياسات:** 24 حالة و6 استعلامات قابلة للإجابة لا تكفي لتقدير جودة إنتاجية. Recall@3 = 1.0 على corpus بهذا الحجم متوقع ولا يدل على تعميم. كذلك دقة no-answer = 1.000 على الجهتين لا تدل على متانة العتبة: استعلامات no-answer في العينة بعيدة موضوعيًا عن الـcorpus (وصفة طبخ، حجز ملعب)، فأي عتبة معقولة تفصلها؛ والحالات الصعبة — استعلام قريب من الموضوع لكن بلا حالة مطابقة — غير ممثلة إطلاقًا. لتقدير موثوق: corpus أكبر، استعلامات حقيقية، عدة بذور، وقياس latency تحت حمل.

## Day 4 — Optimisation and serving decision

**نطاق الادعاء:** artefact_role = **`SYSTEMS_SMOKE`**، result label = **`MEASURED_SMOKE`**. النموذج المقيس هو `google/bert_uncased_L-2_H-128_A-2` برأس تصنيف غير مدرَّب (LABEL_0/1/2) — نموذج تجريبي صغير الهدف منه اختبار المسار لا الجودة. الدفتر يُخرج صراحةً `NEXT_REQUIRED_FOR_GATE_D=RERUN_WITH_PROJECT_ARTIFACT_AND_FULL_WORKLOAD`.

**Budget كُتب قبل قياس المرشحين:** p95 ≤ 1000ms، throughput ≥ 0.1 items/s، quality tax ≤ 0.05، target device = colab-cpu. الميزانية مطبوعة في خلية `TARGET` قبل أي خلية قياس، وموسومة `budget_provenance = COURSE_EXAMPLE_FOR_SYSTEMS_SMOKE`.

**Workload:** 8 صفوف ثنائية اللغة، sha256 = `1d1d1c3bef8a582931f6a1c1803671e8fe194fc36ae59cdc4f4feca9fc4d6785`. نفس الـworkload لكل المرشحين.

**قرار الحشو (dynamic padding):** توزيع الأطوال p50=18، p95=28.95، max=30، بينما `max_length=96`. الحشو الثابت يهدر حسابًا على مواضع فارغة، فاستُخدم dynamic padding بأشكال دفعات `[(4, 30), (4, 26)]` — كل دفعة تُحشى إلى أطول عنصر فيها. `would_truncate = 0` يؤكد أن الحد لا يقطع أي مثال.

**ONNX export و parity:** ONNX checker = PASS. مقارنة PyTorch مقابل ONNX FP32: `max_abs_logits_diff = 2.05e-07` و`prediction_agreement = 1.000` — الفارق في حدود دقة float32 والتنبؤات متطابقة، أي أن **التصدير لم يغيّر السلوك**.

**نتائج القياس [MEASURED_SMOKE]** — p95 model-only، 5 warm-up و30 تكرارًا:

| المرشح | p95 (ms) | الحجم (MiB) | agreement مع FP32 | تسريع |
|---|---:|---:|---:|---:|
| PyTorch FP32 (baseline) | 37.554 | 16.732 | — | 1.00× |
| **ONNX FP32** | **24.653** | 16.788 | **1.000** | **1.52×** |
| ONNX INT8 | 10.031 | 4.287 | **0.625** | 3.74× |

**Quality tax:** المقياس المستخدم هو `prediction_agreement_to_fp32_not_task_quality` — نسبة اتفاق التنبؤات مع المرجع على نفس الأمثلة. **ليس مقياس جودة مهمة**، لأن الرأس غير مدرَّب.

**القرار: `ADOPT_ONNX_FP32`** — ONNX FP32 يخفض p95 بنسبة 34% بتكلفة جودة **صفر**. المرشح يحقق الميزانية كاملة (`budget_met = True`).

**رفض INT8 رغم أنه الأسرع والأصغر:** INT8 أسرع 3.74× وأصغر بأربعة أضعاف، لكن `prediction_agreement = 0.625` يعني اختلاف التنبؤ في **3 من 8 أمثلة** — تكلفة جودة 0.375، أي **سبعة أضعاف حد الميزانية 0.05**. `budget_met = False`. الحجم الأصغر والزمن الأقل لا يبرّران فقدان ثلث التنبؤات. **حجم الملف ليس مقياس جودة.**

**الخدمة (FastAPI + TestClient):** `GET /health` → 200، `POST /v1/classify` عربي وإنجليزي → 200، نص فارغ → **422**، لغة غير مدعومة → **422**. أي العقد يرفض المدخلات غير الصالحة بدل أن يمررها صامتًا.

**Startup canaries:** حالتان تُفحصان عند إقلاع الخدمة — `arabic-contract` ("الخدمة واضحة" → LABEL_2) و`english-contract` ("The service is clear" → LABEL_2). كلتاهما PASS. الـcanary تكشف عند الإقلاع إن كان الـartefact المحمَّل هو المتوقع وإن كان المرمّز يطابق النموذج وlabel map سليمًا — **قبل** استقبال أي طلب حقيقي. فشل عند الإقلاع أرخص بكثير من اكتشاف artefact خاطئ بعد ساعات خدمة.

**ما لا يثبته هذا القياس:**
- artefact_role = SYSTEMS_SMOKE؛ النموذج تجريبي برأس غير مدرَّب ولا يمثل نموذج بيان.
- 8 أمثلة workload — كل مثال يساوي 12.5% في agreement.
- 30 تكرارًا على CPU مشترك في Colab؛ الأزمنة تعتمد على الجلسة.
- لم يُقس throughput تحت تزامن، ولم يُسجَّل `observed peak RSS`.
- `quality tax` هنا اتفاق مع FP32 لا صحة تصنيف.

**بند مفتوح لـGate D:** إعادة التشغيل بـ`PROJECT_ARTIFACT` (نموذج بيان الفعلي) وworkload كامل بمعرّفات ثابتة، مع metric مهمة حقيقي بدل `prediction_agreement`. **لم يُنفَّذ ضمن الوقت المتاح، وهو موثق كنقص معلوم لا كإنجاز.**

**رأس sentiment:** عمود `sentiment` موجود في بيانات اليوم الثاني لكن لم يُدرَّب له رأس مستقل. Gate D يطلب توثيق `topic` و`sentiment` كرأسين منفصلين — الأول منفَّذ ومقيس، والثاني بند مفتوح موثق في `MODEL_CARD.md`.
